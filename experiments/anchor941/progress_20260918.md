# 2026-09-18 项目进度

## 已证实的结果

Kaggle API 确认提交 56290048 已完成，public=0.941。该提交对应 `easoncyy/rsna-anchor941-audited` v1。与此前用户确认的 0.936 相比，公开榜绝对提升 0.005。

另有提交 56297810 同样为 0.941，描述为空；本次未追溯其 Notebook 版本，因此不把它当作独立实验依据。

0.941 为社区配方复现成绩。新增贡献是依赖核查、完整性检查、运行回执和可复现构建，不能将基线模型归为自研。

## 正在推进的实验

Native384-v8 的采样密度由每检查 44 张增加到 64 张。每个窗口由连续 3 张组成，完整缓存下可用中心数分别为 42 与 62。源码实际已遍历原有中心，单独将窗口参数改成 62 会产生重复；因此同步改变真实采样密度与全窗口遍历数。

不改变 6%–94% 的覆盖范围，其他三个 Raptor 分支不变；DINO、A5、RadImageNet、残差 CoAtNet、所有权重及融合比例不变。这检验的是一个分支的采样密度，不是扩大整个模型集合。

构建产物：`native64-notebook/native64.ipynb`；变更证据：`native64_build_receipt.json`。

禁网双 T4 运行已完成，核心流程 171.04 秒，3 个可见病例输出完整。运行回执确认 Native384-v8 为 64 张、62 窗口，其他分支配置符合基线，必需分支全部成功。

已将 v1 提交比赛：编号 **56333692**，时间 **2026-09-18 15:24:12 UTC**（北京时间 23:24:12），最近查询状态 **PENDING**。Notebook：https://www.kaggle.com/code/easoncyy/rsna-anchor941-native64 。本次运行时间受小样本与启动开销影响，不能解释为比基线更快。

## 验收与决策

先验收禁网双 T4 运行、所有分支完整及 `native64_run_receipt.json`，随后提交一个评分对照。可见 3 例只用于验证流程，不用于挑选配方或计算可信准确率。

- 高于 0.941：记录改进，同时保留原基线，后续检验稳健性。
- 显示为 0.941：在当前显示精度下无明确收益，优先保留成本更低的原配置。
- 低于 0.941：回退原配置，停止扩大此分支的采样密度。

仍不恢复 Stage 5A 全类别融合训练。后续研究应围绕强基线确有薄弱证据的类别展开。残差 CoAtNet 回执明确使用 Gold58 选过 checkpoint，因此不能把这 58 例称为该新基线的独立验证集。
