# main 合并审计（2026-09-25）

本次在独立工作目录从远端 `main`（`36d9207`）合并 `dinov2-pseudo-training`（`c1ab030`），不重置原分支。

- 合并前 `main` 有 65 个跟踪文件，功能分支有 352 个；解决冲突后的合并树有 395 个文件，另新增本审计文件，共 396 个。按路径逐一核对，两边各自的跟踪文件均无缺失。
- 早期同名的 `datasets/`、`losses/`、`models/` 模块，以及三份 NLP/伪标签 CSV，在原路径保留 `main` 版本。功能分支原始版本可从合并提交的第二父提交 `c1ab030` 恢复。新阶段的 Notebook、脚本和文档均纳入。
- `.gitignore` 合并两边规则；两张比赛截图同时保留 `info/` 原路径和迁移建议产生的 `docs/archive/` 副本。`info/plan.md` 与两个旧 EDA Notebook 的原路径也保留。
- 此合并是文件结构整合，不把历史的 0.942 Public 分数归因于合并，也不改变正在运行的 Kaggle 任务。Stage 6A 正式训练结果另行审计。

检查：无未解决冲突；新 Stage 6 Python 文件可解析，Notebook/JSON 可解析；本次工作目录无权重或像素缓存进入 Git。历史日志中原有尾随空格不参与实验逻辑。
